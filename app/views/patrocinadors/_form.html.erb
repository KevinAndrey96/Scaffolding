<%= form_with(model: patrocinador, local: true) do |form| %>
  <% if patrocinador.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(patrocinador.errors.count, "error") %> prohibited this patrocinador from being saved:</h2>

      <ul>
      <% patrocinador.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :patrocinador_nombre %>
  </div>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo, id: :patrocinador_tipo %>
  </div>

  <div class="field">
    <%= form.label :telefono %>
    <%= form.number_field :telefono, id: :patrocinador_telefono %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
